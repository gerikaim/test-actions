name: ci-sim

on:
  pull_request:
    branches:
      - main

jobs:
  run-sim:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    - name: Echo
      run: |
        echo "Executing steps for changes in sim"

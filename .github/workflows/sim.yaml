name: ci-sim

on:
  workflow_dispatch:
    inputs:
      ref:
        description: 'Git reference'
        required: true
        default: 'main'

jobs:
  run-sim:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        ref: ${{ github.event.inputs.ref }}

    - name: Run SIM related tasks
      run: |
        echo "Running SIM tasks..."
